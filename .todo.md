# TODO
- [x] Allow config to set cwd of command
    - Use `tokio::fs::canonicalize` to obtain the canocial path to folder
    - `current_dir` fn to set cwd for command
- [x] Look into why processes like python are not exiting
- [] Improve TUI ui
    - [x] Need to figure out the max number of lines that can be displayed for scrolling
        - [x] Figure out max rows per area
    - [] Make it more colorful
        - [x] Added ansi-to-tui crate for ansi color codes
        - [] Add themeing
    - [] Show navigation
    - [] Clearer separation between side bar and output content
        - [x] Added right side border to tab area
    - [] Add regex based highlighting
- [] Add circular buffer for log text (we probably want this?)
- [] Have client register with server the number of processes and their metadata.
- [] Add filter to output
- [] Add output search
- [] LLM integration
    - [] MCP server so that agents can read errors, etc
    - [] Send output to LLM to explain error
- [] Implement output copy
- [] Save output to file
- [] Implement javascript monorepo functionality
    - Scan project folder for package.json
    - Look for dev command in each package
- [] Define different combinations of services to start up
